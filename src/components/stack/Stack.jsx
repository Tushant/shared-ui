import React from "react";
import { Box } from "../box";

export const VerticalStack = (props) => <Box flexDirection="column" {...props} />;
export const HorizontalStack = (props) => <Box flexDirection="row" {...props} />;

